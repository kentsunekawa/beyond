import { css } from 'styled-components'

export const base = css`
  .listHeader {
    display: flex;
    justify-content: flex-end;
    padding: 16px;
  }

  .listFooter {
    display: flex;
    justify-content: flex-end;
    padding: 16px;
  }
`
